# ~/.zshrc - Main entry point for Zsh configuration
# Modular configuration files are sourced from ~/.zsh/

# Source all configuration files in order
for config_file in ~/.zsh/*.zsh(N); do
  source "$config_file"
done
